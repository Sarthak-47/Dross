# Rust
/target/
**/*.rs.bk

# Node
node_modules/
dist/
build/
.vite/

# Python
.venv/
venv/
__pycache__/
*.py[cod]

# Dross's own index (per-repo, regenerable)
.dross/

# Tauri
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/gen/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Benchmark run artifacts: the repository clones and the findings/label files
# are large and regenerable. The harness scripts beside them are neither, and
# the results in docs/ cannot be reproduced without them.
.bench/*
!.bench/*.py

# npm wrapper downloads the native binary at install time
npm/dross-cli-wrapper/bin/dross
npm/dross-cli-wrapper/bin/dross.exe

# Project-local Python environment for the cross-validation linters.
.venv/
