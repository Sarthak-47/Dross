{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core webview permissions plus a folder picker. File reads go through the file_source command, which confines them to the open repository, so no filesystem permission is granted to the renderer.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
